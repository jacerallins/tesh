import { randomUUID } from 'node:crypto';
import type Database from 'better-sqlite3';
import type { Routine, RoutineBridge } from '../../shared/routineTypes';
import type { AuditService } from '../auditService';
export class RoutineService implements RoutineBridge { private readonly routines=new Map<string,Routine>(); constructor(private readonly db:Database.Database,private readonly audit:AuditService,private readonly executeDesktopCommand:(command:string)=>Promise<boolean>=async()=>false){this.load();} async list():Promise<Routine[]>{return [...this.routines.values()].map(r=>({...r,trigger:{...r.trigger},actions:r.actions.map(a=>({...a,input:{...a.input}}))}));} async create(input:Omit<Routine,'id'|'createdAt'|'updatedAt'>):Promise<Routine>{this.validate(input.name,input.trigger.value,input.actions);const now=new Date().toISOString();const routine={...input,id:randomUUID(),createdAt:now,updatedAt:now};this.routines.set(routine.id,routine);this.save(routine);this.audit.record('ROUTINE_CREATED',routine.id);return routine;} async update(id:string,input:Partial<Omit<Routine,'id'|'createdAt'|'updatedAt'>>):Promise<Routine>{const current=this.routines.get(id);if(!current)throw new Error('Routine not found.');const next:Routine={...current,...input,updatedAt:new Date().toISOString(),trigger:{...current.trigger,...(input.trigger??{})},actions:input.actions??current.actions};this.validate(next.name,next.trigger.value,next.actions);this.routines.set(id,next);this.save(next);this.audit.record('ROUTINE_UPDATED',id);return next;} async remove(id:string):Promise<boolean>{if(!this.routines.has(id))return false;this.routines.delete(id);this.db.prepare('DELETE FROM tesh_routines WHERE id=?').run(id);this.audit.record('ROUTINE_DELETED',id);return true;} async run(id:string,confirmed=false):Promise<{ok:boolean;message:string}>{const routine=this.routines.get(id);if(!routine)throw new Error('Routine not found.');if(!routine.enabled)return{ok:false,message:'Routine is disabled.'};for(const action of routine.actions){if(action.requiresConfirmation&&!confirmed)return{ok:false,message:`Confirmation required for ${action.type}.`};if(action.type==='DESKTOP_COMMAND'){const ok=await this.executeDesktopCommand(action.input.command??'');if(!ok)return{ok:false,message:'Desktop command was denied or failed.'};}}this.audit.record('ROUTINE_RUN',id);return{ok:true,message:'Routine completed.'};} private validate(name:string,trigger:string,actions:Routine['actions']):void{if(!name.trim()||name.length>100)throw new Error('Routine name is invalid.');if(trigger.length>200)throw new Error('Routine trigger is invalid.');if(!actions.length||actions.length>20)throw new Error('Routine actions are invalid.');for(const action of actions)if(!action.id||!action.input||typeof action.requiresConfirmation!=='boolean')throw new Error('Routine action is invalid.');} private load():void{this.db.exec('CREATE TABLE IF NOT EXISTS tesh_routines (id TEXT PRIMARY KEY,name TEXT NOT NULL,enabled INTEGER NOT NULL,trigger_json TEXT NOT NULL,actions_json TEXT NOT NULL,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)');const rows=this.db.prepare('SELECT id,name,enabled,trigger_json,actions_json,created_at as createdAt,updated_at as updatedAt FROM tesh_routines').all() as Array<{id:string;name:string;enabled:number;trigger_json:string;actions_json:string;createdAt:string;updatedAt:string}>;for(const row of rows)this.routines.set(row.id,{id:row.id,name:row.name,enabled:Boolean(row.enabled),trigger:JSON.parse(row.trigger_json),actions:JSON.parse(row.actions_json),createdAt:row.createdAt,updatedAt:row.updatedAt});} private save(r:Routine):void{this.db.prepare('INSERT OR REPLACE INTO tesh_routines (id,name,enabled,trigger_json,actions_json,created_at,updated_at) VALUES (?,?,?,?,?,?,?)').run(r.id,r.name,r.enabled?1:0,JSON.stringify(r.trigger),JSON.stringify(r.actions),r.createdAt,r.updatedAt);} }
